<UserControl x:Class="PerSourceAntivirus.Gui.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock FontSize="20" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center">
                <Run Text="Security Alerts ("/>
                <Run Text="{Binding TotalCount}"/>
                <Run Text=")"/>
            </TextBlock>
            <Button Content="Refresh" Command="{Binding RefreshCommand}"
                    Margin="16,0,0,0" Padding="12,6"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     Width="250" Padding="8,6"
                     BorderBrush="#CCCCCC" BorderThickness="1">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <ComboBox SelectedValue="{Binding SelectedType}"
                      Width="160" Margin="12,0,0,0" Padding="8,6">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Lolbin"/>
                <ComboBoxItem Content="Fileless"/>
                <ComboBoxItem Content="Dga"/>
                <ComboBoxItem Content="ProcessHollowing"/>
                <ComboBoxItem Content="AtomBombing"/>
                <ComboBoxItem Content="HeavensGate"/>
                <ComboBoxItem Content="DirectSyscall"/>
                <ComboBoxItem Content="HeapSpray"/>
                <ComboBoxItem Content="ArpSpoofing"/>
                <ComboBoxItem Content="Keylogger"/>
            </ComboBox>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Alerts}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="#FAFAFA"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1"
                  CanUserResizeRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Severity" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center"
                                    Background="{Binding SeverityColor}">
                                <TextBlock Text="{Binding SeverityLabel}" FontSize="11"
                                           FontWeight="Bold" Foreground="White"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Type" Binding="{Binding AlertType}" Width="140"/>
                <DataGridTextColumn Header="Summary" Binding="{Binding TruncatedSummary}" Width="*"/>
                <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="140"/>
                <DataGridTextColumn Header="Detected At"
                                    Binding="{Binding DetectedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                                    Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
