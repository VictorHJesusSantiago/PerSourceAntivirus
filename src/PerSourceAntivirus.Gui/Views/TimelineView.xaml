<UserControl x:Class="PerSourceAntivirus.Gui.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Attack Timeline" FontSize="20" FontWeight="Bold"
                   Foreground="#333" Margin="0,0,0,16"/>

        <!-- Filter bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Process ID (PID)" FontSize="12" Foreground="#555" Margin="0,0,0,4"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox Text="{Binding ProcessIdInput, UpdateSourceTrigger=PropertyChanged}"
                             Width="160" Padding="8,6" FontSize="13" Margin="0,0,8,0"/>
                    <Button Content="Load Timeline" Command="{Binding LoadCommand}"
                            Padding="14,6" Background="{StaticResource AccentBrush}" Foreground="White"
                            BorderThickness="0" Cursor="Hand"/>
                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center"
                               Foreground="{StaticResource AccentBrush}" FontStyle="Italic" FontSize="13"
                               Text="Carregando...">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="#777" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <!-- Timeline tree -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <TreeView ItemsSource="{Binding Nodes}" BorderThickness="0" Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal" Margin="2,2">
                            <TextBlock Text="{Binding Icon}" FontSize="14" Width="20"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Time}" FontSize="11" Foreground="#999"
                                       Width="90" VerticalAlignment="Center" Margin="4,0,8,0"
                                       FontFamily="Consolas"/>
                            <TextBlock FontSize="13" VerticalAlignment="Center" TextWrapping="NoWrap"
                                       MaxWidth="700" TextTrimming="CharacterEllipsis">
                                <TextBlock.Text>
                                    <Binding Path="Label"/>
                                </TextBlock.Text>
                                <TextBlock.Foreground>
                                    <Binding Path="Color"/>
                                </TextBlock.Foreground>
                            </TextBlock>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True"/>
                        <Setter Property="Padding" Value="2"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
