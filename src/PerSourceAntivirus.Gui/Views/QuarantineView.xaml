<UserControl x:Class="PerSourceAntivirus.Gui.Views.QuarantineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PerSourceAntivirus.Gui.Views">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quarantine" FontSize="18" FontWeight="Bold"
                   Foreground="#333" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" BorderThickness="1" BorderBrush="#DDD"
                  Background="White" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="180"/>
                <DataGridTextColumn Header="Original Path" Binding="{Binding OriginalPath}" Width="*"/>
                <DataGridTextColumn Header="Quarantined At" Binding="{Binding QuarantinedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="140"/>
                <DataGridTextColumn Header="SHA-256" Binding="{Binding Hash}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Restore Selected" Padding="16,8"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="OnRestoreClick"
                    IsEnabled="{Binding SelectedItem, Converter={x:Static local:NullToBoolConverter.Instance}}"/>
        </StackPanel>
    </Grid>
</UserControl>
