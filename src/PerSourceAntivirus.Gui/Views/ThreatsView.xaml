<UserControl x:Class="PerSourceAntivirus.Gui.Views.ThreatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Threats Detected" FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource ContentTextPrimary}" Margin="0,0,0,12"/>

        <!-- Search bar -->
        <TextBox Grid.Row="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                 Padding="8" Margin="0,0,0,12" FontSize="13"
                 BorderBrush="#DDD" BorderThickness="1">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Resources>
                        <VisualBrush x:Key="HintBrush" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="Search threats..." Foreground="{DynamicResource ContentTextMuted}" FontSize="13" Padding="2,0,0,0"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Threats list -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Threats}" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" BorderThickness="0" Background="{DynamicResource CardBackground}"
                  RowBackground="{DynamicResource CardBackground}" AlternatingRowBackground="{DynamicResource SubtleBackground}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="180"/>
                <DataGridTextColumn Header="Path" Binding="{Binding FilePath}" Width="*"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StatusColor}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="YARA Rule" Binding="{Binding YaraRule}" Width="160"/>
                <DataGridTextColumn Header="Detected" Binding="{Binding DetectedAt, StringFormat={}{0:MM/dd HH:mm}}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
