<UserControl x:Class="PerSourceAntivirus.Gui.Views.ExclusionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Exclusion Management" FontSize="20" FontWeight="Bold"
                   Foreground="#333" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Text="{Binding NewExclusion, UpdateSourceTrigger=PropertyChanged}"
                     Width="400" Padding="8,6"
                     BorderBrush="#CCCCCC" BorderThickness="1"/>
            <Button Content="Add" Command="{Binding AddCommand}"
                    Margin="8,0,0,0" Padding="16,6"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,0">
            <ListBox ItemsSource="{Binding Exclusions}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="0,6">
                            <Grid>
                                <TextBlock Text="{Binding}" FontSize="13" Foreground="#333"
                                           VerticalAlignment="Center"/>
                                <Button Content="×" HorizontalAlignment="Right"
                                        Command="{Binding DataContext.RemoveCommand,
                                            RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"
                                        Width="28" Height="28"
                                        Background="{StaticResource DangerBrush}" Foreground="White"
                                        BorderThickness="0" Cursor="Hand" FontSize="16"
                                        VerticalContentAlignment="Center"
                                        HorizontalContentAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Import from File" Command="{Binding ImportCommand}"
                    Padding="12,6" Background="#555" Foreground="White"
                    BorderThickness="0" Cursor="Hand" Margin="0,0,8,0"/>
            <Button Content="Export to File" Command="{Binding ExportCommand}"
                    Padding="12,6" Background="#555" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>
