<UserControl x:Class="PerSourceAntivirus.Gui.Views.ExclusionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Exclusion Management" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource ContentTextPrimary}" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Text="{Binding NewExclusion, UpdateSourceTrigger=PropertyChanged}"
                     Width="400" Padding="8,6"
                     AutomationProperties.Name="Caminho ou padrão a excluir"
                     AutomationProperties.HelpText="Informe um arquivo ou pasta que o antivírus deve ignorar"
                     BorderBrush="{DynamicResource ContentBorder}" BorderThickness="1"/>
            <Button Content="Preview de Impacto" Command="{Binding PreviewImpactCommand}"
                    Margin="8,0,0,0" Padding="16,6"
                    Background="#EEEEEE" Foreground="{DynamicResource ContentTextPrimary}"
                    BorderThickness="0" Cursor="Hand"/>
            <Button Content="Add" Command="{Binding AddCommand}"
                    Margin="8,0,0,0" Padding="16,6"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding ImpactPreviewMessage}"
                   FontSize="12" Foreground="{DynamicResource ContentTextSecondary}" TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,0,0,0">
            <ListBox ItemsSource="{Binding Exclusions}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource ContentBorder}" BorderThickness="0,0,0,1" Padding="0,6">
                            <Grid>
                                <TextBlock Text="{Binding}" FontSize="13" Foreground="{DynamicResource ContentTextPrimary}"
                                           VerticalAlignment="Center"/>
                                <!-- Icon-only button: "×" conveys nothing to a screen reader, so the
                                     accessible name has to be supplied explicitly (WCAG 4.1.2). -->
                                <Button Content="×" HorizontalAlignment="Right"
                                        Command="{Binding DataContext.RemoveCommand,
                                            RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="Remover exclusão"
                                        ToolTip="Remover esta exclusão"
                                        Width="44" Height="44"
                                        Background="{StaticResource DangerBrush}" Foreground="White"
                                        BorderThickness="0" Cursor="Hand" FontSize="16"
                                        VerticalContentAlignment="Center"
                                        HorizontalContentAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Import from File" Command="{Binding ImportCommand}"
                    Padding="12,6" Background="#555" Foreground="White"
                    BorderThickness="0" Cursor="Hand" Margin="0,0,8,0"/>
            <Button Content="Export to File" Command="{Binding ExportCommand}"
                    Padding="12,6" Background="#555" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>
